import styled from "styled-components";

export const HeaderWrapper = styled.div`
  display: flex;
  align-items: center;
  flex-wrap: nowrap;
  flex-direction: column;
  padding: 0 10px 10px;
  margin: 0 auto;
  text-align: center;
  padding: 0 10px 20px;
`;

export default {
  HeaderWrapper,
};
